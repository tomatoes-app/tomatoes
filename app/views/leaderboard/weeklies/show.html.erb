<h1><%= t('weekly_leaderboard') %></h1>

<p><%= render partial: 'leaderboard/links' %></p>

<div class="leaderboard_list">
  <%= render partial: 'leaderboard/leaderboard', locals: { scores: @scores } %>
</div>
